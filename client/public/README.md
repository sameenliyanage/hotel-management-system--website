Place your hotel logo image file here as `logo.png`.

- Save the attached logo you provided earlier to this folder and name it `logo.png`.
- The printable invoice uses `/logo.png` so the file should be available at `client/public/logo.png` when the dev server runs.

If you prefer not to add a file, tell me and I can embed the image as a base64 data URL in the printable HTML instead.